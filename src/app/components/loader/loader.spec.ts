import { LoaderComponent } from './loader.component';

describe('Loader-component testing for', () => {
  it('initializing', () => {
    let component = new LoaderComponent();
    expect(component).toBeDefined();
  })
})
